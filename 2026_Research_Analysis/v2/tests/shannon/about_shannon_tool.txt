# Instructions
1) Open "tool1_shannon_entropy.html"
2) Provide probabily character CSV file or generate your own (header line is expected).
3) Provide simple CSV of domains (no header necessary)
4) Open the output file "shannon_entropy_results.csv"
5) Adjust the "Verdict" column with "benign" or "evil" as a baseline of what we know. This column is manually edited by the analyst, so be sure to change this.
6) Feed the "shannon_entropy_results.csv" (edited) file back to "tool2_avg_entropy.html" to see the overall spread.
7) Export the "shannon_entropy_flagged_YYYY-MM-DD.csv".
8) Rename the flagged CSV export as necessary.

It is acknowledged that Shannon Entropy is a relative and sliding scale that usually needs live data, but this gives a characterization of the corpus.

These two HTML tools were generated by Claude AI.